const name = 'Gangzar agas S';
const fullName = `Nama saya adalah: ${name}`;

console.info(fullName);

const namaSiswa = `Agas`;
let ipk = 3.19;
const keterangan = `Siswa dengan nama ${namaSiswa}, Lulus : ${ipk >= 1.00} `;
console.info(keterangan);